# AngularSampleApp
Hello Parixit Code

Hide .js and .map files when working after setting it in setting.json file

   // Workspace settings
    "files.exclude": {
            "**/*.js":  {"when": "$(basename).ts"},
            "**/*.map": true
    }

Routing
indexRoute
fallbackRoute

